import { WebPartContext } from "@microsoft/sp-webpart-base";

export interface IDelPhiQuickLinksProps {
  tilesImageUrl: string;
  listName:string;
  context:WebPartContext;
  isBackGround:boolean;
  backGroundColor:string;
  
}
